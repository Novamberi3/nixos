// Placeholder entrypoint — quickshell looks for a top-level qml file like
// this inside the config dir it's pointed at (~/.config/quickshell/nova).
// Replace with your actual shell once you have one.

import Quickshell

ShellRoot {
}
